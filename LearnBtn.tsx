/*import React from 'react'

type ButtonProps = {
  props: {
    backgroundColor: string;
    fontSize: number;
    textColor: string;
  }//property style should exist inside of the type ButtonProps so prop name below and prop in type should be same.
}//above props is already an object referred as a parent object of all properties passed to LearnBtn.If you want to destruct spesific object inside props parent,use {props}
const LearnBtn = ({ props } : ButtonProps) => {
  //style={style} means we have style object inside of style attribute
  return (
<button style={props}>click me</button>
  )
}

export default LearnBtn*/
